# Sub2API 变更记录 — 2026-04-16 ~ 2026-04-17

## 概览

本次工作包含两个 feature 分支的合并、多轮 bug 修复、Cloudflare R2 配置、以及生产部署。

---

## 一、新功能

### 1. Cache Creation Token 虚增 (`feat/cache-creation-inflate`)

**目的：** 对上游 Anthropic API 返回的 `cache_creation_input_tokens` 按分组配置进行虚拟膨胀，用于调整计费。

**公式：** `final = int(original × (1 + percent/100) + fixed)`

**改动范围：**
- Ent schema: `groups` 表新增 `cache_creation_inflate_percent` (DECIMAL) 和 `cache_creation_inflate_fixed` (DECIMAL)
- 全链路覆盖：handler → service → repository → auth cache → 前端
- 支持所有响应路径：流式 SSE、非流式 JSON、passthrough、非 passthrough、OpenAI 转换、Gemini 转换
- 仅对 `platform = "anthropic"` 的分组生效
- 前端 Admin Groups 页面新增两个输入框

**关键文件：**
- `backend/ent/schema/group.go` — schema 定义
- `backend/internal/service/group.go` — `InflateCacheCreationTokens()` 方法
- `backend/internal/service/gateway_service.go` — 流式/非流式 inflate 逻辑
- `backend/internal/service/openai_gateway_service.go` — OpenAI 路径 inflate
- `backend/internal/handler/admin/group_handler.go` — Admin API
- `frontend/src/views/admin/GroupsView.vue` — 前端配置 UI
- `backend/migrations/108_add_cache_creation_inflate_to_groups.sql`

### 2. 审计日志存储到 Cloudflare R2 (`feat/audit-log-r2`)

**目的：** 将 API 网关的完整请求/响应审计日志保存到 S3 兼容对象存储（Cloudflare R2）。

**设计：**
- Gin 中间件捕获响应体（最大 16MB，可配置）
- 异步上传队列（5000 容量）+ 单后台 goroutine
- JSON gzip 压缩
- Admin 健康检查端点 `/api/v1/admin/ops/audit-log/health`

**关键文件：**
- `backend/internal/server/middleware/audit_log.go` — 中间件
- `backend/internal/service/audit_log_sink.go` — 异步上传队列
- `backend/internal/repository/audit_log_s3_store.go` — S3 客户端
- `backend/internal/config/config.go` — `AuditLogConfig` 配置结构

---

## 二、Bug 修复（按发现时间排序）

### Fix 1: 审计日志泄露 API 密钥
**提交：** `2fa61622`
**问题：** `cloneHeaders()` 直接复制所有请求头到 R2，包含 `Authorization`、`x-api-key` 等敏感信息。
**修复：** 新增 `sensitiveHeaders` 映射表，匹配到的 header 替换为 `[REDACTED]`。
**文件：** `backend/internal/server/middleware/audit_log.go`

### Fix 2: Cache inflate 未膨胀 TTL 分桶
**提交：** `2fa61622`
**问题：** `inflateCacheCreationJSON()` 只膨胀了 `cache_creation_input_tokens` 聚合值，未膨胀 `cache_creation.ephemeral_5m/1h_input_tokens`。计费逻辑（LiteLLM 动态定价启用 `SupportsCacheBreakdown` 时）优先用分桶值，导致 inflate 对计费无效。
**修复：** inflate 后将 `ephemeral_5m_input_tokens` 设为膨胀后的聚合值。
**文件：** `backend/internal/service/gateway_service.go`

### Fix 3: 流式响应 usage 解析顺序
**提交：** `2fa61622`
**问题：** Anthropic/OpenAI passthrough streaming 先解析 usage 再 inflate，导致计费用原始值、客户端看膨胀值。
**修复：** 调换顺序：先 inflate → 再 parseSSEUsage。
**文件：** `backend/internal/service/gateway_service.go`, `openai_gateway_service.go`

### Fix 4: Bucket-only payload 不触发 inflate
**提交：** `34923286`
**问题：** `inflateCacheCreationJSON()` 要求 `cache_creation_input_tokens` 字段存在才执行。如果上游只返回 `ephemeral_5m/1h` 没有聚合字段，inflate 被跳过。
**修复：** 如果聚合字段不存在，从 5m+1h 合成。
**文件：** `backend/internal/service/gateway_service.go`

### Fix 5: SSE 路径未记录原始值
**提交：** `34923286`
**问题：** 流式路径先 inflate 再 parse usage，`OriginalCacheCreationInputTokens` 未被设置（0）。
**修复：** 在 inflate 前读取原始值并存入 `usage.OriginalCacheCreationInputTokens`。
**文件：** `backend/internal/service/gateway_service.go`

### Fix 6: `GetByKeyForAuth` 遗漏 inflate 字段（根因 Bug）
**提交：** `d83362e7`
**问题：** API key 认证查询的 `WithGroup` Select 没有包含 `CacheCreationInflatePercent` 和 `CacheCreationInflateFixed`，导致 auth cache snapshot 中这两个值永远为 0，**流式 inflate 完全不生效**。
**发现过程：** 部署后通过 DB 查询发现 `original_cache_creation_tokens` 始终为 0。加 debug 日志确认 group 的 inflate 值正确（从 gin context 读取），最终定位到 Ent Select 遗漏字段。非流式路径碰巧走了不同的查询（`GetByID`），所以能生效。
**修复：** 在 Select 列表中添加 `group.FieldCacheCreationInflatePercent` 和 `group.FieldCacheCreationInflateFixed`。
**文件：** `backend/internal/repository/api_key_repo.go`

### Fix 7: 计费与 inflate 不一致（5m 分桶问题）
**提交：** `36629d06` → `7166564f`
**问题：** LiteLLM 动态定价启用了 `SupportsCacheBreakdown`，计费走 `computeCacheCreationCost()` 时用 `5m_tokens` 而非聚合值。inflate 只给 5m 加了 delta（不是完整的膨胀值），导致计费偏低。
**发现过程：** 审计一条请求发现 `cache_creation_cost` 远低于预期。反算得出计费用了 `5m_tokens=292` 而非 `aggregate=3214`。
**修复：** inflate 后将 `ephemeral_5m_input_tokens` 设为膨胀后的聚合值（不是只加 delta），`ephemeral_1h_input_tokens` 置 0。
**文件：** `backend/internal/service/gateway_service.go`, `openai_gateway_service.go`

---

## 三、前端改动

### Admin Usage 面板显示 inflate 信息
**提交：** `d66f7252`
- Token 列旁显示紫色 `+N` 标签（仅当 `original_cache_creation_tokens > 0` 且与 `cache_creation_tokens` 不同时）
- Tooltip 中显示原始值和虚增量
**文件：** `frontend/src/components/admin/usage/UsageTable.vue`, `frontend/src/types/index.ts`

---

## 四、基础设施 & 部署

### Cloudflare R2 配置
- Bucket: `sub2api-audit-logs`（Account: `f6e7ec63e6cdc362c4ef042cbf45c0f8`）
- S3 API Token: 通过 Dashboard 创建（Account API Token，Read & Write）
- Endpoint: `https://f6e7ec63e6cdc362c4ef042cbf45c0f8.r2.cloudflarestorage.com`

### 生产部署流程（64.186.230.163）
1. DB migration 先行（`ALTER TABLE ADD COLUMN ... DEFAULT` — 零停机，旧版兼容）
2. 二进制上传为 `sub2api.new`（不替换旧的）
3. `config.yaml` 追加 `audit_log` 段
4. `mv sub2api sub2api.old && mv sub2api.new sub2api && systemctl restart sub2api`（~2s 停机）
5. 观测验证：日志无报错、usage_logs 正常写入、R2 审计日志初始化成功、余额正常扣费

### 回滚方案
```bash
# 30 秒内回滚
cd /opt/sub2api
mv sub2api sub2api.new && mv sub2api.old sub2api
cp config.yaml.bak config.yaml
systemctl restart sub2api
# DB 列不需要回滚（旧二进制用显式列名 INSERT，新列有 DEFAULT）
```

### 余额安全保证
- **扣费和日志分离** — `applyUsageBilling` 原子事务先执行，`writeUsageLogBestEffort` 后执行且失败不影响扣费
- **去重保护** — `usage_billing_dedup` 表 + `ON CONFLICT DO NOTHING`
- **DB migration 向后兼容** — 全是 `ADD COLUMN ... NOT NULL DEFAULT`
- **回滚安全** — 旧代码 47 列 INSERT 到有 48 列的表，新列有 DEFAULT

---

## 五、发现的架构特点

1. **计费链路**：`applyUsageBilling()` 原子事务 → `writeUsageLogBestEffort()` 异步最佳努力。扣费永远不会因为日志写入失败而丢失。
2. **API Key Auth Cache**：L1 内存（15s TTL）→ L2 Redis（5min TTL）→ DB 回源。Snapshot 有 version 字段，版本不匹配自动回源。
3. **Ent Select 陷阱**：`GetByKeyForAuth` 使用显式 Select 字段列表（性能优化），新增字段必须手动加入，否则读到零值。`GetByID` 没有 Select 限制所以不受影响。
4. **LiteLLM 动态定价**：`pricingService` 优先于硬编码 fallback 价格。当 `CacheCreationInputTokenCostAbove1hr > CacheCreationInputTokenCost` 时自动启用 `SupportsCacheBreakdown`，计费走 5m/1h 分桶而非聚合值。
5. **Group 软删除**：`SoftDeleteMixin` 自动过滤 `deleted_at IS NOT NULL`。删除一个 group 后，引用它的 `fallback_group_id` 会导致更新校验失败（404）。

---

## 六、Git 提交记录

```
7166564f fix: set ephemeral_5m to inflated aggregate for consistent billing
36629d06 fix: inflate delta always goes to ephemeral_5m_input_tokens
d66f7252 feat: show cache creation inflate delta in admin usage panel
d83362e7 fix: add cache inflate fields to GetByKeyForAuth select query
34923286 fix: support bucket-only inflate and record original tokens in SSE paths
2fa61622 fix: cache inflate TTL bucket support, audit log header redaction, and streaming usage order
2e974bd1 Merge branch 'feat/audit-log-r2'
04381d3a feat: add cache creation token inflation per user group
e07e0aad feat: add full request/response audit logging to Cloudflare R2
cc9dbd28 chore: add CLAUDE.md for Claude Code guidance
```

---

## 七、待办 / 已知限制

- [ ] 流式路径 `OriginalCacheCreationInputTokens` 在原始值为 0 时不记录（`origCC > 0` 条件跳过）
- [ ] Audit log sink 单线程上传 + 5k 队列，高流量时可能静默丢弃
- [ ] Audit log sink 关闭时有 Submit/Stop 竞态条件（可丢日志）
- [ ] 部署停机约 2 秒，200 RPM 下约丢 6-7 个请求。可通过 nginx 反向代理实现零停机
